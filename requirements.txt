# requirements.txt — Proyecto MoE Medical Vision Router
# Versiones fijadas para reproducibilidad en el cluster del curso (CUDA 11.8 / 12.1).
# Instalar: pip install -r requirements.txt

# ---------- PyTorch stack ----------
torch==2.2.2
torchvision==0.17.2
torchmetrics==1.3.2

# ---------- Vision / Transformers ----------
timm==0.9.16
einops==0.7.0

# ---------- Medical imaging ----------
monai==1.3.1
SimpleITK==2.3.1
nibabel==5.2.1
scikit-image==0.22.0

# ---------- Ciencia de datos ----------
numpy==1.26.4
pandas==2.2.1
scipy==1.12.0
scikit-learn==1.4.1.post1
matplotlib==3.8.4
seaborn==0.13.2

# ---------- Imagen ----------
Pillow==10.2.0
opencv-python-headless==4.9.0.80

# ---------- Ablation k-NN ----------
faiss-cpu==1.8.0

# ---------- Dashboard (Streamlit + FastAPI) ----------
streamlit==1.32.2
fastapi==0.110.1
uvicorn==0.29.0
python-multipart==0.0.9

# ---------- Hugging Face (descarga de pesos) ----------
huggingface_hub==0.22.2

# ---------- Grad-CAM ----------
grad-cam==1.5.0

# ---------- Imbalanced / stratification (NIH) ----------
imbalanced-learn==0.12.2
iterative-stratification==0.1.7

# ---------- Utilidades ----------
tqdm==4.66.2
openpyxl==3.1.2

# ---------- Notas ----------
# - Para GPU: instalar torch con indice CUDA apropiado:
#     pip install torch==2.2.2+cu118 torchvision==0.17.2+cu118 --index-url https://download.pytorch.org/whl/cu118
# - En Colab las dependencias base ya estan; solo falta:
#     !pip install -q monai timm einops SimpleITK nibabel faiss-cpu grad-cam
